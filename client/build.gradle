//
//description = ''
//dependencies {
//    compile group: 'javax.ws.rs', name: 'javax.ws.rs-api', version:'2.0.1'
//    compile group: 'javax.enterprise', name: 'cdi-api', version:'1.2'
//    compile group: 'com.googlecode.gentyref', name: 'gentyref', version:'1.1.0'
//    testCompile group: 'org.glassfish.jersey.core', name: 'jersey-client', version:'2.17'
////  testCompile project(':application')
//    testCompile group: 'junit', name: 'junit', version:'4.11'
//    testCompile group: 'org.mockito', name: 'mockito-core', version:'1.10.8'
//    testCompile group: 'org.hibernate', name: 'hibernate-validator', version:'5.1.3.Final'
//    compile(group: 'javax.validation', name: 'validation-api', version:'1.1.0.Final') {
//       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
//       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
//       Please review and delete this closure when resolved. */
//    }
//}

ascelion {
	java {
		main {
			source = JavaVersion.VERSION_1_7

			artifactId = "${rootProject.name}-${project.name}"
		}
	}
}

dependencies {
	compile( dep.gentyref )

	provided( dep.jee7.cdi )
	provided( dep.jee7.jax_rs )
	provided( dep.jee7.validation )

	testCompile( project( ':tests:application' ).sourceSets.main.output )
	testCompile( dep.junit )
	testCompile( dep.mockito.all )

	testRuntime( dep.jersey.client )
	testRuntime( dep.hibernate.validator )
}
